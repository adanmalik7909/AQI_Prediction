# requirements-webapp.txt
# For: webapp/ (Phase 10 - Streamlit dashboard)
#
# GOOD NEWS: streamlit < 1.35 requires protobuf <5,>=3.20, which OVERLAPS
# with hopsworks' requirement of protobuf <5.0,>=4.25.4 (both are happy
# with anything in the 4.25.4-5.0 range). This means the web app can be
# installed into the SAME aqi_env as the rest of the pipeline - no need
# for a separate environment, and the web app can use the real Hopsworks
# SDK (genuine Feature Store + Model Registry access, as the project
# requires) instead of hand-rolling REST calls.
#
# Install directly into aqi_env:
#   pip install -r requirements-webapp.txt

streamlit<1.35
flask